package tiles;

/**
 * The list of all the outcomes that interacting with a tile can produce
 */
public enum TileInteractionResults {
    //#region enum entries
    SUCCESS,
    EXPLOSION,
    INVALID_TILE;
    //#endregion
}
